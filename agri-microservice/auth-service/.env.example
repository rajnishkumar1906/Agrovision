# Paseto Configuration
# Generate using: node -e "import('./utils/tokenUtils-paseto.js').then(m => m.generatePasetoSecret().then(s => console.log(s)))"
PASETO_SECRET_KEY=your-paseto-secret-key-in-hex-format-64-chars
PASETO_EXPIRE=604800